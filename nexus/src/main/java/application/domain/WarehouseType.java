package application.domain;

/**
 * Classification of a Warehouse. Matches Domain 4: distinction between
 * Marketplace warehouses and Seller warehouses.
 */
public enum WarehouseType {
    MARKETPLACE,
    SELLER
}
